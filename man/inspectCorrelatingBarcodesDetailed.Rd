% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BarcodePairCorrelation.R
\name{inspectCorrelatingBarcodesDetailed}
\alias{inspectCorrelatingBarcodesDetailed}
\title{Visualising the proportion trajectories of barcodes within each cluster,
after running the \code{clusterCorrelatingBarcodes} function.}
\usage{
inspectCorrelatingBarcodesDetailed(
  barbieQ,
  transformation = "none",
  ncol = NULL
)
}
\arguments{
\item{barbieQ}{A \code{barbieQ} object with information of clusters
of correlating barcodes, which should be predicted by the
\link{clusterCorrelatingBarcodes} function, after initialized the barbieQ object
by the \link{createBarbieQ} function.}

\item{transformation}{A character string specifying the transformation
applied to barcode proportions before plotting. One of \code{"none"} (default),
\code{"asin-sqrt"}, or \code{"logit"}. Should match the transformation used in
\link{clusterCorrelatingBarcodes} for consistency.}

\item{ncol}{An integer specifying the number of columns in the facet layout.
Defaults to \code{NULL}, which automatically arranges facets in an approximately
square grid based on the number of clusters.}
}
\value{
A \code{ggplot} object with one facet per cluster, showing per-barcode
proportion trajectories across samples. Samples are ordered by mean
proportion within each cluster. Unclustered barcodes (cluster <= 0) are
excluded.
}
\description{
\code{inspectCorrelatingBarcodesDetailed()} visualizes per-barcode proportion
trajectories across samples, faceted by cluster, for clustered barcodes
generated by calling \code{clusterCorrelatingBarcodes()}.
}
\examples{
nbarcodes <- 50
nsamples <- 12
count <- abs(matrix(rnorm(nbarcodes * nsamples), nbarcodes, nsamples))
rownames(count) <- paste0('Barcode', seq_len(nbarcodes))
barbieQ <- createBarbieQ(count)
barbieQ <- clusterCorrelatingBarcodes(barbieQ, preDefinedCluster = list(
 group1 = c('Barcode1', 'Barcode2', 'Barcode3'), group2 = c('Barcode4', 'Barcode5')))
inspectCorrelatingBarcodesDetailed(barbieQ)
}
